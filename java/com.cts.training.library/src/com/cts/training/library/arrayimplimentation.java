package com.cts.training.library;

public class arrayimplimentation {
	public float averageofmul5(int n ) {
		int[]arr
	float avg=0;
		int sum=0,len,count=0;
		len=arr.length;
		for(byte i=0;i<len;i++)
		{
			if(arr[i]%5==0) {
				sum+=arr[i];
				count++;
			}
			}
		System.out.println(count);
		try {
			avg=sum/count;
		}
		finally {
			return avg;
		}
		}
		
public int minvalposition(int n) {
	
	}
	
}

 
}
